@page
@model WCSTrainer.Pages.Locations.DeleteModel

@{
   ViewData["Title"] = "Delete";
}

<div class="panel">
   <div class="body">
      <component type="typeof(Sidebar)" render-mode="WebAssemblyPrerendered" />
      <div class="properties">
         <div class="user-form">
            <p class="title">Location #@Html.DisplayFor(model => model.Location.Id)</p>
            <div class="field-container">
               <div class="text-group">
                  <p class="title">Name</p>
                  <p class="info">@Html.DisplayFor(model => model.Location.Name)</p>
               </div>
               <br />
               <form method="post" class="form-group">
                  <input type="hidden" asp-for="Location.Id" />
                  <input type="submit" value="Delete" class="btn bg-btn btnRed" />
               </form>
            </div>
         </div>
      </div>
   </div>
</div>